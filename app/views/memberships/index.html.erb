<% @memberships.each do |membership| %>
  <div class="container">
    <div class="row">
      <div class="col-lg-4">
        <div class="well">
          <p><%= link_to membership.membership_type, membership_path(membership) %></p>
        </div>
      </div>
    </div>
  </div>
<% end %>
